# 乾淨官子最佳一手 — 實用 cheatsheet

> 一頁版。不證明、不討論複雜度、不講三種 D 讀法。給定一個「乾淨」盤面，照公式套出 (x, y)。
>
> 理論依據在 `proof.md` / `math-solution.md`，要看再去看。

---

## 適用條件（缺一即不適用）

- 不含劫
- 各空地 region 互不影響（無共享氣、無跨區威脅）
- 各 region 棋形屬於常見類型（corridor、switch、已成定地）
- 沒有未活的大塊

殘局後段大致都符合；中盤還有大 fight 不要用。

---

## 四步公式

```
給盤面 →
  1. 把空地切成幾塊（不相連的 region）：{R_1, R_2, ..., R_m}
  2. 對每塊算「目數差」：
       Δ_i  =  (黑先動該塊的最終結果)  −  (白先動該塊的最終結果)
  3. 找 Δ 最大的那塊：R*
  4. 答案 = R* 的 key point (x, y)
```

要 top-k：取 Δ 前 k 大的 region 各自的 key point。

---

## 三種常見形狀之 Δ 速查表

### (1) Corridor 1×ℓ（邊空通道）

雙端對方活子封閉、ℓ 個連續空點。

| ℓ | 黑先得 (LS) | 白先得 (RS) | **Δ** | key point |
|---|------------|------------|------|-----------|
| 1 | 1 | 0 | **1** | 唯一空點 |
| 2 | 2 | 0 | **2** | 開放端 |
| 3 | 3 | 1 | **2** | 開放端 |
| 4 | 4 | 2 | **2** | 開放端 |
| 5 | 5 | 4 | **1** | 開放端 |
| 6+ | $\ell$ | $\lfloor (\ell-1)^2/4 \rfloor$ | 通常 $\leq 0$ | 已 cold |

通用公式：$\Delta = \ell - \lfloor (\ell-1)^2/4 \rfloor$.

### (2) Simple switch $\{a \mid b\}$（兩種確定結局的爭奪點）

關鍵點唯一，雙方搶下會分別導致兩種已知結局。

- **Δ = a − b**
- **key point = 該爭奪點本身**（即 $\{a \mid b\}$ 的 hot point）

例：「先動方多 8 目、被動方損 8 目」 → $\Delta = 8 - (-8) = 16$.

### (3) Number（已成定地）

雙方都不會去下。

- **Δ = 0**
- 不用考慮

---

## 例子

盤面分解出三塊：

| Region | 形狀 | $\Delta$ |
|--------|------|---------|
| R1 | corridor 長 3 | 2 |
| R2 | corridor 長 5 | 1 |
| R3 | switch $\{8 \mid -8\}$ | 16 |

排序：$R_3$ (16) > $R_1$ (2) > $R_2$ (1).

**最好一手**：$R_3$ 的 key point $(x, y)$.
**第 2 手**：$R_1$ 的 key point.
**第 3 手**：$R_2$ 的 key point.

---

## 三個邊界提醒

1. **平手怎麼辦**：兩塊 $\Delta$ 一樣大（例：corridor ℓ=2, 3, 4 都 Δ=2）→ 任一都行，或挑後續威脅較大的（這時要用直覺，公式分不出）。
2. **看到 follow-up**：某塊下完後白會反擊到另一塊 → 已不在「乾淨官子」範圍，請查 `proof.md` §5.6 之 Hotstrat 失效反例。
3. **不確定形狀**：若眼睛無法分類（不像 corridor 也不像 switch）→ 退回實戰直覺，或丟給 KataGo 等引擎。

---

## 一句話總結

**算每塊 region 的 Δ（黑先 − 白先），下 Δ 最大那塊的 key point。**

公式：

$$(x^*, y^*) \;=\; m^*\big( \arg\max_i \Delta(R_i) \big)$$
